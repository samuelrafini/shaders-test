    // if (waveShaderMaterialRef.current?.uniforms) {
    // //   waveShaderMaterialRef.current.uniforms = {
    // //     ...waveShaderMaterialRef.current?.uniforms,
    // //     uTime: { value: clock.getElapsedTime() }
    // //   }
    // }

    props: JSX.IntrinsicElements['mesh']